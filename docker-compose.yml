services:
  mercurjs:
    build:
      context: .
    image: mercurjs
    container_name: mercurjs-dev
    network_mode: host
    volumes:
      - bun-cache:/root/.bun/install/cache
      - npm-cache:/root/.npm

volumes:
  bun-cache:
  npm-cache: