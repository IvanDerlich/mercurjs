#!/bin/bash
# Acceso directo para generar el comando npx de mercurjs
# Modifica ENV_DIR para apuntar a la carpeta donde está tu .env

ENV_DIR="."

# Llama al generador de comando npx
"$(dirname "$0")/scripts/generar_comandos/npx_install.sh" "$ENV_DIR"
